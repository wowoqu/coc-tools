# 部落换卡助手

一个移动端优先的静态换卡工具：选择 1 张可以提供的重复卡，再一次勾选所有缺少的卡片，可选填写玩家与部落信息，然后导出固定尺寸的换卡分享图。

## 功能

- 全宽原创奇幻部落 Banner
- 四类 60 张兵种卡配置和名称/标签搜索
- “1 张重复卡 → 多张缺卡”的两步选择流程
- 只能选择与重复卡相同类别的缺卡
- 缺卡可连续勾选、取消与一键清空
- 预览与导出图完整展示每张已选卡的图片和名称
- 自动排除相同卡片
- 玩家名称、部落名称和部落 ID 选填，输入时实时更新海报
- 刷新后恢复当前选择
- 导出 `1080 × 1350` PNG
- 手机、平板和桌面端响应式布局
- 无账号、无接口、无数据库

## 技术栈

- Vue 3 + TypeScript + Vite
- Vue Router（Hash 模式）
- Element Plus（按需导入）
- SCSS
- html-to-image
- 轻量 Composition API 状态，不使用 Pinia/Vuex

目前状态只有一张重复卡、缺卡 ID 数组、当前步骤和玩家/部落信息，没有引入状态库。若以后增加多活动、跨页面编辑、分享链接或后端同步，再考虑迁移到 Pinia。

## 本地运行

建议使用 Node.js 20.19 或更高版本，以及 pnpm 10 或更高版本。

```bash
pnpm install
pnpm dev
```

生产构建：

```bash
pnpm build
```

## 修改活动与卡片

业务配置位于：

```text
src/config/activity.ts
```

每个卡组由 `id`、名称、说明、排序、强调色和卡片数组组成。当前配置为圣水 19 张、暗黑重油 13 张、建筑大师基地 11 张、超级兵种 17 张。增加卡组或卡片不需要修改页面组件。

卡片支持以下信息：

- 唯一 ID
- 名称
- 所属分组
- 高清图片路径（可选）
- 文字图形占位
- 主题色
- 卡片类别
- 标签
- 禁用状态与原因

60 张卡均已接入真实游戏兵种素材，没有使用 AI 生成或仿画兵种图。素材文件放在：

```text
public/images/cards/
```

并在配置中使用 `/images/cards/xxx.webp` 路径。素材保存在项目内并与页面同源，避免浏览器 Canvas 跨域限制；图片加载异常时仅显示文字占位，不生成替代角色图。

## Banner

当前 Banner 位于：

```text
public/images/banner/clan-war-banner.png
```

它是为本项目生成的原创奇幻部落场景，不包含官方 Logo 或对现有角色的直接复制。手机端通过不同的 `object-position` 自动裁切同一张图。

## 图片导出

海报组件使用固定的 `540 × 675` 逻辑尺寸，导出时生成 `1080 × 1350` PNG。因此导出结果不会受到手机屏幕宽度影响。

玩家与部落信息规则：

- 玩家名称、部落名称和部落 ID 均最多 20 个字符
- 支持中英文、数字、空格和 Emoji
- 自动去除换行
- 导出时去除首尾空格并合并连续空格
- 留空的项目不会在海报顶部渲染

## 项目结构

```text
src/
├─ assets/styles/       全局样式与设计变量
├─ components/          Banner、卡片、步骤、海报等组件
├─ composables/         轻量换卡状态
├─ config/              活动与卡片配置
├─ router/              页面路由
├─ types/               TypeScript 类型
├─ utils/               PNG 导出逻辑
└─ views/               选卡页与预览页
```

## 说明

本项目是非官方玩家工具，未经 Supercell 认可或赞助。公开发布或部署前，应再次核对 [Supercell Fan Content Policy](https://supercell.com/en/fan-content-policy/) 与素材授权要求。
